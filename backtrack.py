def backtrack(board):

    # Pick first valid guess
        # guess = (num, i, j)

    # Solve

    # If no violations done
    # If violation, undo, new call
    # If stuck, call backtrack

    return
